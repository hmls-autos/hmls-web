{
  "name": "hmls",
  "version": "0.3.0",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=hmls-web",
    "dev:api": "cd apps/api && deno task dev",
    "dev:diagnostic": "cd apps/diagnostic-agent && deno task dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "version:sync": "bun run scripts/sync-versions.ts",
    "version:check": "bun run scripts/sync-versions.ts --check",
    "release": "bun run scripts/release.ts",
    "docker:build": "./scripts/docker-build.sh",
    "docker:push": "./scripts/docker-build.sh --push"
  },
  "devDependencies": {
    "turbo": "^2.7.5"
  }
}
